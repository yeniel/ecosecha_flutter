name: domain
description: A new Flutter package project.
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: '^3.1.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  ###########################
  # PLUGINS AND PACKAGES

  equatable: any
  rxdart: any
  shared_preferences: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any

flutter:
